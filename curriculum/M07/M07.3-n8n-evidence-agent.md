# M07.3 — n8n Evidence Agent chỉ đọc

## Mục tiêu

Dùng AI Agent để research/summarize/propose trong read-only ceiling. MCP là candidate cho interoperability, không thay permission model.

## Nội dung cốt lõi

Import `lab/n8n/M07-readonly-evidence-agent.blueprint.json`. Chỉ nối tool read-only; output cuối vẫn phải qua grounding/tool-boundary evaluator.

## Failure cases

- bỏ qua authority ceiling của Mission;
- coi tool/model success là business truth;
- làm mất provenance/version/correlation cần audit;
- dùng capability của Mission sau để vượt gate hiện tại.

## PASS

Bạn tự chạy eval/runtime tương ứng, giải thích được failure case và lưu operated evidence theo starter của Mission.
