# M04 Operated Evidence (bằng chứng vận hành)

- Evidence IDs + source/observed_at (mã bằng chứng + nguồn/thời điểm quan sát):
- Model/provider/version (nếu dùng LLM thật):
- AdvisorOutput (đầu ra tư vấn):
- as_of + max_age (mốc đánh giá + tuổi tối đa):
- hallucinated-evidence test (kiểm thử bằng chứng bịa):
- stale/future-evidence test (kiểm thử bằng chứng cũ/tương lai):
- abstain test (kiểm thử từ chối kết luận):
- write-request test (kiểm thử yêu cầu ghi):
- limitation (giới hạn):
- next measurement (phép đo tiếp theo):
