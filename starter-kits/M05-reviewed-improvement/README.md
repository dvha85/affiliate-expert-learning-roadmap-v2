# Bộ khởi đầu — M05

## Thứ tự

1. Học các bài trong `curriculum/M05/`.
2. Chạy `cd lab/mission-runtime && go test ./...`.
3. Chạy `go run ./cmd/demo M05`.
4. Đọc/chạy eval pack `evals/M05-reviewed-improvement/`.
5. Lưu operated evidence cá nhân dưới `learner/M05/` (không commit dữ liệu nhạy cảm).

## Checklist + operated evidence

Từ evaluation thật tạo ImprovementProposal có version/risk/rollback; review thủ công; không auto-apply.

Ghi tối thiểu: predicted result, observed result, một failure case, evidence/reality boundary, limitation, next measurement.
