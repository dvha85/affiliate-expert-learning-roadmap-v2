# M07 Operated Evidence (bằng chứng vận hành)

- Model/provider/version (mô hình/nhà cung cấp/phiên bản):
- Tool Registry (sổ đăng ký tool):
- Source allowlist (danh sách nguồn cho phép):
- Evidence IDs (mã bằng chứng):
- Normal Agent run + correlation_id (lần chạy Agent bình thường + mã liên kết):
- Unknown-tool test (kiểm thử tool không đăng ký):
- Write-method test (kiểm thử phương thức ghi):
- Hallucinated-evidence test (kiểm thử bằng chứng bịa):
- Prompt-injection/tool-output test (kiểm thử chèn chỉ dẫn/đầu ra tool):
- Grounding/HUMAN_REVIEW output (đầu ra bám bằng chứng/cần người review):
- limitation (giới hạn):
- next measurement (phép đo tiếp theo):
