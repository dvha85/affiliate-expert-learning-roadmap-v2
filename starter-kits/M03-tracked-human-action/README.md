# Bộ khởi đầu — M03

## Thứ tự

1. Học các bài trong `curriculum/M03/`.
2. Chạy `cd lab/mission-runtime && go test ./...`.
3. Chạy `go run ./cmd/demo M03`.
4. Đọc/chạy eval pack `evals/M03-tracked-human-action/`.
5. Lưu operated evidence cá nhân dưới `learner/M03/` (không commit dữ liệu nhạy cảm).

## Checklist + operated evidence

Ghi một ActionRecord human-only, một OutcomeRecord sau measurement window, disclosure/compliance review và explain-back về zero vs missing.

Ghi tối thiểu: predicted result, observed result, một failure case, evidence/reality boundary, limitation, next measurement.
